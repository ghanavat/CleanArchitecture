namespace CleanArchitecture.Api.Tests;

#pragma warning disable CS1591

public class Tests
{
    public Tests()
    {
        /*
         * If you want to test whether any class implements an interface, you can do the following:
         * bool implementsInterface = typeof(IMyInterface).IsAssignableFrom(typeof(MyClass));
         * Or:
         * Type interfaceType = typeof(MyClass).GetInterface("IMyInterface");
         * 
         */
    }
}
